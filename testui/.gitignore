# Local config holds the MQTT password (0600) — never commit it.
# Use config.example.toml as the template; supply the password via the
# EnvironmentFile on the deploy host.
config.toml

# Build artifacts
bin/
dist/
/testui

# Local Claude settings
.claude/