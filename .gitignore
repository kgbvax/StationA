# macOS / local tooling noise
.DS_Store
.claude/
# local model-provider env scratch
/claude_code_zai_env.sh

# Compound Engineering machine-local config (example config stays tracked)
.compound-engineering/*.local.yaml
