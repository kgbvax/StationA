# Compiled binaries (cross-compile output and local dev build).
bin/
/flexbridge
/flexbridge-*

# Local config overrides (keep deploy/config.example.toml, ignore real config).
config.toml
*.local.toml

# Editor / OS cruft.
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Test artifacts.
coverage.out
*.test
