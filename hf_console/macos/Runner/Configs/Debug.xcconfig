#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"
// Optional per-developer signing overrides (DEVELOPMENT_TEAM etc.).
// Copy Developer.xcconfig.example to Developer.xcconfig — it is gitignored.
#include? "Developer.xcconfig"